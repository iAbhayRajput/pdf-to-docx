<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF to DOCX Converter</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .custom-file-upload {
            cursor: pointer;
            background-color: #4a5568;
            color: #ffffff;
            border: none;
            border-radius: 0.25rem;
            margin-top: 2%;
            padding: 0.5rem 1rem;
            transition: background-color 0.3s ease;
        }

        .custom-file-upload:hover {
            background-color: #2d3748;
        }
    </style>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full p-8 bg-gray-800 shadow-md rounded-lg">
        <h1 class="text-2xl font-bold mb-4">PDF to DOCX Converter</h1>
        <form action="/convert" method="post" enctype="multipart/form-data">
            <div class="mb-4">
                <label for="pdfFile" class="block text-sm font-medium mb-4">Choose a PDF file:</label>
                <label for="pdfFile" class="custom-file-upload">
                    <input type="file" name="pdfFile" id="pdfFile" accept=".pdf" class="hidden" onchange="document.getElementById('fileName').innerText = this.files[0].name">
                    Choose File
                </label>
                <p id="fileName" class="mt-2 text-sm text-gray-300"></p>
            </div>
            <button type="submit" class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">Convert</button>
        </form>
    </div>
</body>
</html>
